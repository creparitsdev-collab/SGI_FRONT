import { Spinner as HeroUISpinner } from "@heroui/react";

export const Spinner = () => {

    return (
        <>
            <HeroUISpinner
                size='sm' 
                color='current'
            />
        </>
    );
}